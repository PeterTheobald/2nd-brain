[project]
name = "2nd-brain"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "discord.py==2.3.2",
    "anthropic>=0.25.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "python-frontmatter>=1.1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
